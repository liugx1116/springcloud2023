server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.gjt.mm.mysql.Driver # 当前数据源操作类型
    url: jdbc:mysql://localhost:3306/study2023??useUnicode=true&characterEncoding=utf-8&useSSL=false # mysql驱动包
    data-username: root
    data-password: root123

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.atgx.springcloud.entities # 所有Entity别名类所在包
